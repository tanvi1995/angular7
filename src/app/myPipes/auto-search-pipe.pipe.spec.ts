import { AutoSearchPipePipe } from './auto-search-pipe.pipe';

describe('AutoSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AutoSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
